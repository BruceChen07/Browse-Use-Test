[project]
name = "browseusetest"
version = "0.1.0"
description = "A Browser Use agent example project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "browser-use>=0.11.3",
    "python-dotenv>=1.0.1",
]
